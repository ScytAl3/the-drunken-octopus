{{ form_start(form) }}

{{ form_row(form.name) }}

{{ form_row(form.address) }}

{{ form_row(form.address2) }}

<div class="row">
    <div class="col">
        {{ form_row(form.zipcode) }}
    </div>
    <div class="col">
        {{ form_row(form.city) }}
    </div>
    <div class="col">
        {{ form_row(form.country) }}
    </div>
</div>

{{ form_row(form.phone) }}

{{ form_row(form.sameForBilling) }}

{# Validation button #}
<div class="my-4">
    <input type="submit" class="btn btn-primary btn-lg btn-block"
           value="{{ submitButtonText ?? ('my_account.address.form.buttons.create' | trans ({}, 'pagecontent')) }}">
</div>

{{ form_end(form) }}