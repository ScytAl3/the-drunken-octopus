{{ form_start(form, {attr: {class: 'filter js-filter-form'}}) }}

{# Icone spinner pour indiqué la prise en compte du filtre #}
<div class="spinner-border js-spinner-loading" role="status" aria-hidden="true" style="display: none;">
    <span class="sr-only">
        {{ 'products.filters.spinner' | trans ({}, 'pagecontent') }}
    </span>
</div>

{# champ search #}
<div>
    {{ form_row(form.q) }}
</div>

{# champ style #}
<h4 class=" border-bottom border-info">
    {{ 'products.filters.style' | trans ({}, 'pagecontent') }}
</h4>
<div>
    {{ form_row(form.style) }}
</div>

{# champ country #}
<h4 class=" border-bottom border-info">
    {{ 'products.filters.country' | trans ({}, 'pagecontent') }}
</h4>
<div>
    {{ form_row(form.country) }}
</div>

{# champ brewery #}
<h4 class=" border-bottom border-info">
    {{ 'products.filters.brewery' | trans ({}, 'pagecontent') }}
</h4>
<div>
    {{ form_row(form.brewery) }}
</div>

{{ form_end(form) }}