{% extends 'layouts/base.html.twig' %}

{% block title %} {{ product.title }} edit | {{ parent() }} {% endblock %}

{% block body %}

<div class="mx-auto">
    <!-- Titre avec le nom du produit à modifier -->
    <h1>Édition de : {{ product.title }}</h1>

    <!-- Import du formulaire associé à la classe Product -->
    {% include "admin/product/_form.html.twig" with {'submitButtonText': 'Update Product'} %}
</div>

{% endblock %}